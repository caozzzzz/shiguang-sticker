---
name: shiguang-sticker
description: 将用户上传的旅行、城市、自然风景、街景、交通或建筑照片，重新绘制为“拾光贴图”收藏级旅行记忆海报；支持 3:4 竖版或 4:3 横版，包含忠实于原图的主插画、恰好 6 枚手账贴图、地点时间落款和简短中文记忆描述。原图仅作为参考，不直接出现在成品中。
---

# 拾光贴图

把照片重新表达为“旅行明信片 × 手账贴纸页”，而不是把照片直接排进海报。核心优先级：用户明确要求 > 原图真实性 > 地点与时间可靠性 > 主体辨识度 > 原构图记忆 > 六枚贴纸真实性 > 文字规则 > 风格与装饰。

## 开始前

1. 检查是否有至少一张可用照片。每张照片都要独立分析，不能把上一张照片里的对象带入下一张。
2. 确认版式：竖版严格为 3:4，横版严格为 4:3。用户未指定时，必须先问“这张旅行记忆想生成竖版还是横版？”。
3. 获取省份、城市和拍摄时间。优先使用用户提供的信息，其次使用可靠的 GPS、DateTimeOriginal 等原始元数据；不得根据景物猜测，也不得把文件创建、修改或下载时间当作拍摄时间。缺少完整信息时先询问用户。
4. 处理记忆描述：用户提供且不超过 20 个汉字时保留原文（明显错字或用户要求润色除外）；超过 20 个汉字时自动提炼；未提供时根据画面生成一句 8–16 字、最多 20 个汉字的克制中文描述。

用户提供的信息已足够时，不重复询问。

## 照片分析与真实性

生成前内部确定：

- `Identification Anchor`：最具辨识度且代表这段记忆的真实主体。
- 环境、前中远景、地平线、光线、天气、色彩和主要空间关系。
- 原图中真实可见的辅助对象与人物活动；人物重绘为无五官剪影。
- 5–8 种保持原图冷暖关系的低饱和主色。
- `STICKER SOURCE MAP`：逐一列出 6 枚贴纸及其在原图中的视觉依据。

主插画和贴纸里的每个具体对象必须在原图中真实可见，或由用户明确要求加入。不得为了丰富画面、填补空白、凑齐类别或改善构图而虚构人物、动物、交通工具、建筑、道路、太阳、月亮、飞鸟、花朵等对象。允许简化细节和艺术化重绘，但要保留主锚点的轮廓、比例、主要结构、标志色与空间关系。

原始照片只能作为参考：禁止直接展示、嵌入、拼贴、裁切为贴纸、用作背景，或在成品中保留摄影质感区域。成品所有视觉元素必须统一重绘。

## 生成

根据版式读取并遵守 [references/layout-and-style.md](references/layout-and-style.md)，根据用户与元数据生成文字时读取 [references/text-rules.md](references/text-rules.md)。构建图像生成提示词时依次明确：参考照片、真实性分析、主锚点、环境与真实辅助对象、氛围、构图、配色、六枚贴纸来源、版式、地点时间、记忆描述、艺术风格及负面约束。

调用可用的图像生成或图像编辑工具，并把用户上传的所有目标照片作为参考图传入。图像工具若不能稳定渲染中文，可在提示词中进一步强调只允许两层指定文字并要求准确复制；不要擅自改成英文或添加标题。

## 生成后检查

读取 [references/quality-checklist.md](references/quality-checklist.md) 逐项检查最终图像。重点核对比例、主锚点、对象真实性、贴纸数量、文字内容与多余文字。若硬约束失败，调整提示词并重新生成；不要把明显不合格的结果当作完成品交付。

用户要求局部修改时，只覆盖相应的非核心参数。无论如何修改，都继续遵守来源真实性、可靠元数据、恰好 6 枚贴纸及只保留两层指定文字等核心约束。
